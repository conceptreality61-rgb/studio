
'use client';
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function WorkerProfilePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Worker Profile</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is the worker profile page. Details about the worker will be displayed here.</p>
      </CardContent>
    </Card>
  );
}
